// TODO: Send Email with payload that could not have been inserted
export const sendMail = (tx_id: string, text: string, related_to: string) => {
/*
  email.send({
    to          : ,
    subject     : "Message Error",
    text        : `
        tx_id: ${tx_id}
        source: ${(messages[0] || {}).source}
        related_to: ${(messages[0] || {}).related_to}
    `,
    attachments : [{
        contentType : "text/json",
        fileName    : tx_id + ".json",
        contents    : Buffer.from(JSON.stringify(messages), "utf8")
    }]
})
*/
}
      